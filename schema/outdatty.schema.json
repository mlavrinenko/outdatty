{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Manifest",
  "description": "A declared dependency graph between artifacts.",
  "type": "object",
  "properties": {
    "gitignore": {
      "description": "When true (the default), glob expansion skips paths ignored by git — the `.gitignore` files (root and nested), the global excludes, and `.git/info/exclude` — so build artifacts and other generated files never enter a group. Set to false to match every file on disk.",
      "default": true,
      "type": "boolean"
    },
    "groups": {
      "description": "Dependency groups. Each group couples one or more source artifacts to the dependents that must be re-confirmed when a source changes.",
      "default": [],
      "type": "array",
      "items": {
        "$ref": "#/definitions/Group"
      }
    },
    "require_tracked": {
      "description": "Glob patterns for files that must be covered by some group's `source` or `dependents`. Defaults to `[\"**\"]`, so every file git does not ignore must belong to a group. Patterns are matched last-wins: a later pattern overrides an earlier match and a `!` prefix excludes, so `[\"**\", \"!vendor/**\"]` requires everything but `vendor/`, and `[\"src/**\"]` requires only `src/`. The manifest and lockfile themselves are always exempt. Set to `[\"!**\"]` to opt out entirely.",
      "default": [
        "**"
      ],
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "additionalProperties": false,
  "definitions": {
    "Group": {
      "description": "A single dependency group.",
      "type": "object",
      "required": [
        "name",
        "source"
      ],
      "properties": {
        "bidirectional": {
          "description": "When true, a change to any dependent also marks the group out of date, modelling a bidirectional coupling.",
          "default": false,
          "type": "boolean"
        },
        "dependents": {
          "description": "Dependent artifacts that must be re-confirmed when a source changes. Defaults to empty for a source-only group with nothing to review.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "name": {
          "description": "Stable identifier used in reports and for `--group` targeting. Required and unique: lockfile entries are keyed by it, so it must not depend on a group's position in the manifest.",
          "type": "string"
        },
        "source": {
          "description": "Source artifacts. A change to any source marks the group out of date. Entries may be literal paths or glob patterns such as `src/**/*.rs`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    }
  }
}
